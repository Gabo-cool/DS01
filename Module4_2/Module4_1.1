#include <iostream>
#include <stack>
#include <vector>
#include <string>
using namespace std;

int main()
{
    string mystring;
    int openbracecounter = 0;
    int closebracecounter = 0;
    stack<int, vector<int>> iStack;

    cout << "Enter a string of braces: ";
    cin >> mystring;

    for (int x = 0; x < mystring.size(); x++)
    {
        cout << "Pushing " << x << endl;
        iStack.push(mystring[x]);
        if(x = isEmpty; x++, x = mystring)
        {
            cout << x << endl;
        }
        else if (currentChar == '}')
        {
            cout << "close brace ";
            closebracecounter++;
        }
    }
}
   stack.Display
   cout << "Open braces: " << openbracecounter << endl;
   cout << "Close braces:" << closebracecounter << endl;
   
   int replacements = openbracecounter - closebrcecounter; 
   cout << "Minimum of replacements required: " replacements << endl;
